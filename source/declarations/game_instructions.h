#ifndef GAME_INSTRUCTIONS_H
#define GAME_INSTRUCTIONS_H

#include <stdbool.h>
#include <SDL2/SDL.h>

bool show_instructions(SDL_Renderer* renderer);
// shows user game instructions
// return value: true if user wants to quit

#endif
